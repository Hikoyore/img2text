# img2text
Converting images to text (tags) for stable diffusion

```python
python -m venv venv
```

```python
pip install -r requirements.txt
```

```python
python tagger.py
```

остальное оформление будет позже.
